import React from "react";
import { Button } from "react-bootstrap";

const Profile = () => {
  return (
    <div>
      <Button variant="warning" className="rounded-circle mx-3">
        P
      </Button>
    </div>
  );
};

export default Profile;
